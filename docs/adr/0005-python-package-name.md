# ADR 0005: Python distribution name

Status: accepted for preparation, registry ownership pending

Use `pinout-client` for the daemon SDK because the unscoped `pinout` name may be owned by an unrelated project. Registry availability and ownership must be checked by the release owner before publication; local builds are the only validation in this repository.
