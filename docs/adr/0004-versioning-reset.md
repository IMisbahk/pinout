# ADR 0004: Alpha version line

Status: accepted for preparation, publication deferred

Prepare the package line as `0.0.1-alpha.1` and retain the historical `v0.4.0` tag. No tag or registry operation is performed by local checks. Future alpha increments must update npm and Python metadata together.
