{
  "name": "@pinout/mcp",
  "version": "0.1.0",
  "description": "MCP stdio server for Pinout — exposes device capabilities as agent tools.",
  "type": "module",
  "bin": {
    "pinout-mcp": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -b"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@pinout/core": "*"
  }
}
